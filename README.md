# data-markup

> **Deprecated** — this package has been folded into [termkit](https://www.npmjs.com/package/termkit). Please migrate to `termkit` instead.
>
> ```sh
> npm install termkit
> ```

A Node.js module for pretty-printing structured data (objects, arrays, dates, primitives) with colorized terminal output.
